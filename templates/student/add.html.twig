<!DOCTYPE html>
<html>
<head>
    <title>Ajouter un étudiant</title>
</head>
<body>
<h1>Ajouter un étudiant</h1>

<form method="post" action="{{ path('student_create') }}">
    {{ form_start(form) }}

    {{ form_row(form.name, {'label': 'Nom'}) }}
    {{ form_row(form.email, {'label': 'Email'}) }}
    {{ form_row(form.age, {'label': 'Âge'}) }}
    {{ form_row(form.classroom, {'label': 'Classe'}) }}

    <button type="submit" class="btn btn-primary">Ajouter</button>

    {{ form_end(form) }}
</form>

<a href="{{ path('fetch') }}">Retour à la liste des étudiants</a>
</body>
</html>